services:
  web:
    build: .
    restart: unless-stopped
    environment:
      VIRTUAL_HOST: findr.beautiful-apps.com
      VIRTUAL_PORT: "80"
      LETSENCRYPT_HOST: findr.beautiful-apps.com
    networks:
      - proxy

networks:
  proxy:
    external: true
